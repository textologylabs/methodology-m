# scaffold-repo

Create and configure a managed repo from a sub-task file

## Triggers

scaffold repo, create managed repo

## Execution

Read and execute the full capability at `{{M_ROOT}}/capabilities/scaffold-repo/SKILL.md`.

Follow the M execution protocol defined in `{{M_ROOT}}/m.md`.
