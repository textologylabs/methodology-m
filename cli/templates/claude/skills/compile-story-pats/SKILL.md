# compile-story-pats

Compile a story-level PAT into an executable CAT spec and raise the
root-repo integration MR.

## Triggers

compile story pat, compile story pats, raise integration gate

## Execution

Read and execute the full capability at `.m/capabilities/compile-story-pats/SKILL.md`.

Follow the M execution protocol defined in `.m/m.md`.
