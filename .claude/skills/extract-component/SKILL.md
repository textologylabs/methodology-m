# extract-component

TYPE-CHANGE `embedded → referenced` — extract an embedded component into its own managed repo

## Triggers

extract component, make component referenced, embedded to referenced, type-change extract

## Execution

Read and execute the full capability at `.m/capabilities/extract-component/SKILL.md`.

Follow the M execution protocol defined in `.m/m.md`.
