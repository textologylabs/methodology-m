# embed-component

TYPE-CHANGE `referenced → embedded` — fold a referenced component back into the root repo

## Triggers

embed component, fold component into root, referenced to embedded, type-change embed

## Execution

Read and execute the full capability at `.m/capabilities/embed-component/SKILL.md`.

Follow the M execution protocol defined in `.m/m.md`.
