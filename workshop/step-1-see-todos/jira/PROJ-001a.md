# PROJ-001a: Implement GET /todos endpoint

## Parent
PROJ-001 — User can see a list of todos

## Component
todo-api-read

## Description
Implement the GET /todos endpoint that returns the list of todos.

## Acceptance Criteria
- GET /todos returns 200 with array of objects containing title and completed fields
- GET /todos returns empty array when no todos exist
